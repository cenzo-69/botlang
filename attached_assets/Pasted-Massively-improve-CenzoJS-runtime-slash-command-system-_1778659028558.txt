Massively improve CenzoJS runtime, slash command system, interaction system, and error handling.

IMPORTANT:
- do NOT break existing runtime/parser/interpreter
- keep AST execution and nested functions working
- check if functions already exist before creating new ones
- do NOT overwrite working implementations

━━━━━━━━━━━━━━━━━━━━
SLASH COMMAND SYSTEM
━━━━━━━━━━━━━━━━━━━━

Add full slash command support.

Need:
txt id="m7v2x4" interactionCreate slash commands buttons select menus modals autocomplete context menus 

Architecture should support:
txt id="q4m8x2" message commands slash commands interactions buttons modals 

through ONE runtime/context system.

Add:
txt id="v1m7q8" context.message context.interaction context.component context.modal 

Functions should work in BOTH:
- message commands
- slash commands

Example:
txt id="x8m2q4" $username 

should work for:
- messages
- slash interactions
- buttons
- modals

without rewriting functions.

━━━━━━━━━━━━━━━━━━━━
SLASH COMMAND FILE EXAMPLES
━━━━━━━━━━━━━━━━━━━━

Need examples and handler examples so users can learn the framework.

Example slash command:

js id="r2m7v1" module.exports = {   name: "ping",   description: "Shows bot ping",   type: "slash",   options: [     {       name: "user",       description: "Target user",       type: "user",       required: false     }   ],   code: `   Pong: $botPing   ` } 

Button example:

js id="n5x8m2" module.exports = {   name: "testButton",   type: "button",   customID: "test",   code: `   Hello $username   ` } 

Modal example:

js id="j4m7q9" module.exports = {   name: "feedbackModal",   type: "modal",   customID: "feedback",   code: `   Thanks for feedback   ` } 

Also create:
- slash command registration system
- interaction router
- event loader
- event examples
- proper docs/examples for users

━━━━━━━━━━━━━━━━━━━━
EVENT SYSTEM
━━━━━━━━━━━━━━━━━━━━

Add event architecture:

Example:
js id="k8v2m5" module.exports = {   name: "guildMemberAdd",   type: "event",   code: `   Welcome $username   ` } 

Need support for:
txt id="w2m8q4" messageCreate guildMemberAdd guildMemberRemove interactionCreate voiceStateUpdate ready guildCreate guildDelete messageDelete messageUpdate 

━━━━━━━━━━━━━━━━━━━━
ERROR HANDLING SYSTEM
━━━━━━━━━━━━━━━━━━━━

Current errors are bad.

Need professional error system:
- parser errors
- runtime errors
- function errors
- permission errors
- syntax errors
- missing argument errors
- invalid argument errors
- unknown function errors

Errors should show:
txt id="f7m2q1" function name line column call stack argument position error type 

Example:

txt id="t4m7q2" RuntimeError: Function: $purge Line: 4 Argument: amount Reason: Invalid number 

Need colorful/clean console errors and optionally Discord error embeds.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT RUNTIME FIX
━━━━━━━━━━━━━━━━━━━━

Discord action functions should NOT return unnecessary text.

Example bad behavior:
txt id="y1m8q4" $purge[5] 

currently returns:
txt id="u7x2m1" 5 

which leaks into message output.

Action functions like:
txt id="p9m2x4" $purge[] $ban[] $kick[] $timeout[] $deleteMessage[] $addRole[] $removeRole[] 

should return:
txt id="a1v8q7" "" 

unless explicitly configured otherwise.

Need cleaner runtime output system.

━━━━━━━━━━━━━━━━━━━━
ONLYIF IMPROVEMENTS
━━━━━━━━━━━━━━━━━━━━

Improve:
txt id="b4m7q2" $onlyIf[] 

Need support for:
txt id="c8m2x1" == != > < >= <= && || 

Examples:
txt id="d7v2m4" $onlyIf[$message==hi;Say hi] $onlyIf[$authorID!=$ownerID;Owner only] $onlyIf[$memberCount>=10;Need 10 members] 

Need proper condition parser/evaluator instead of simple string checks.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT OUTPUT BUG
━━━━━━━━━━━━━━━━━━━━

This code:

txt id="e1m8q2" $purge[$noMentionMessage] 

should NOT send:
txt id="f4v7q8" 5 

above embeds.

Action functions should silently execute unless returning output is intended.

Fix runtime aggregation/output system.