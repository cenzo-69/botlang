Add these features 
$displayName[userID?]
$userBanner[userID?]
$userCreated[userID?]
$userJoined[userID?]
$userRoles[userID?;separator]
$userAvatar[userID?]
$userMention[userID?]
$isBot[userID?]

$serverName
$serverID
$serverOwnerID
$serverCreated
$serverBoostCount
$serverBoostLevel
$serverVanity
$serverVerificationLevel

$createChannel[name;type]
$deleteChannel[channelID]
$setChannelName[channelID;name]
$slowmode[channelID;seconds]

$createRole[name;color]
$deleteRole[roleID]
$addRole[userID;roleID]
$removeRole[userID;roleID]
$hasRole[userID;roleID]

$ban[userID;reason]
$kick[userID;reason]
$timeout[userID;time;reason]
$untimeout[userID]
$purge[amount]

$button[label;customID;style]
$selectMenu[id;placeholder]
$modal[id;title]
$textInput[id;label;style]

$cooldown[10s]
$globalCooldown[5m]

$onlyPerms[ManageMessages]
$onlyRoles[roleID]
$onlyChannels[channelID]
$onlyUsers[userID]

Also add BDFD-style block conditions.

Current:
txt id="m7v2x4" $if[condition;then;else] 

Need support for REAL block syntax:

txt id="q4m8x2" $if[condition] hello $elseif[condition] test $else fail $endif 

Example:

txt id="v1m7q8" $if[$message==hi] Hello $elseif[$message==hello] Hey $else Unknown $endif 

Requirements:
- nested if blocks
- nested loops inside if blocks
- elseif chaining
- proper parser block tracking
- lazy branch execution
- only execute matching branch
- support multiline blocks
- support recursive AST execution
- proper syntax errors for missing $endif

Need parser/interpreter architecture for:
- block nodes
- branch nodes
- execution scopes

Do NOT fake this with regex.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Also improve:
txt id="x8m2q4" $onlyIf[condition;errorMessage] 

BDFD-style behavior:

Example:
txt id="r2m7v1" $onlyIf[$message==hi;You must say hi] 

If false:
- stop execution immediately
- send error message
- prevent remaining code execution

If true:
- continue execution normally

Need:
- proper execution halt system
- works inside nested functions
- works inside loops
- works inside eval
- supports nested function conditions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Important:
This requires REAL parser/interpreter support for:
- block parsing
- control flow nodes
- lazy execution
- execution stopping
- scoped branch execution

Do NOT implement using dumb string slicing or regex hacks.