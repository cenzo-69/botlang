'use strict';

// $textInput[customID;label;style?]
//
// Text inputs are components of modals, which require an interaction context.
// This function is a no-op stub. Use $textInput inside a $modal handler.
// style: short (default) | paragraph
module.exports = async (context, args) => {
  return '';
};
